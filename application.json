{
  "displayName": "Hello World",
  "defaultColor": "#14b8a6",
  "description": "The reference Frontier application — one small, heavily-commented example of every capability: server, worker, ui, bus (private/public), Store, Config, MCP, Scheduler, Hooks, and the worker-channel fan-out.",
  "dataVersion": 2
}
