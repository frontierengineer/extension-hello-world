{
  "//": "Compiler SETTINGS for the BROWSER half. Matches how the host bundles ui/: DOM lib, react-jsx, bundler resolution. Like the root tsconfig, this provides settings only — `../../types` resolves above a flat repo, so `npm run verify` runs tsc against the production-nested mirror. See README → 'How types resolve'.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["index.tsx"]
}
